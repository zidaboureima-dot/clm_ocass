name: clm_ocass
description: CLM/OCASS Guinée - Suivi dirigé par les communautés
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.27.0
  firebase_auth: ^4.19.4
  cloud_firestore: ^4.15.5
  firebase_storage: ^11.0.0

  # UI
  google_fonts: ^6.2.1

  # Utilitaires
  intl: ^0.19.0
  uuid: ^4.3.3
  cloud_functions: ^4.7.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
